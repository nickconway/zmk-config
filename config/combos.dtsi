combos {

    compatible = "zmk,combos";

    /* #include "combo_shift.dtsi" */

    combo_hmrl_shift {
        timeout-ms = <50>;
        key-positions = <13 25>;
        bindings = <SK(LSHFT)>;
        layers = <COLE>;
    };

    combo_hmrl_gui {
        timeout-ms = <50>;
        key-positions = <14 26>;
        bindings = <SK(LGUI)>;
        layers = <COLE>;
    };

    combo_hmrl_alt {
        timeout-ms = <50>;
        key-positions = <15 27>;
        bindings = <SK(LALT)>;
        layers = <COLE>;
    };

    combo_hmrl_ctrl {
        timeout-ms = <50>;
        key-positions = <16 28>;
        bindings = <SK(LCTL)>;
        layers = <COLE>;
    };

    combo_hmrr_ctrl {
        timeout-ms = <50>;
        key-positions = <19 31>;
        bindings = <SK(LCTL)>;
        layers = <COLE>;
    };

    combo_hmrr_alt {
        timeout-ms = <50>;
        key-positions = <20 32>;
        bindings = <SK(LALT)>;
        layers = <COLE>;
    };

    combo_hmrr_gui {
        timeout-ms = <50>;
        key-positions = <21 33>;
        bindings = <SK(LGUI)>;
        layers = <COLE>;
    };

    combo_hmrr_shift {
        timeout-ms = <50>;
        key-positions = <22 34>;
        bindings = <SK(LSHFT)>;
        layers = <COLE>;
    };

    combo_none {
        timeout-ms = <50>;
        key-positions = <13 14 15 16 19 20 21 22>;
        bindings = <&tog NOTHING>;
        layers = <COLE NOTHING>;
    };

};
